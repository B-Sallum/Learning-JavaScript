const cores = ['Azulis', 'Indigos', 'Branque', 'Black', 'White']

let index = 0

while (index < cores.length) {
    console.log(index, cores[index])
    index = index + 1  
}   




let index2 = 0;

while (index2 < cores.length) {
    console.log(index2, cores[index2]);
    index2++  ;       
}


